apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-ai-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-ai-service
  template:
    metadata:
      labels:
        app: python-ai-service
    spec:
      containers:
      - name: python-ai-service
        image: skillforge-python-service:latest
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: python-ai-service
spec:
  selector:
    app: python-ai-service
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
